using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SandBox
{
    class TestGhostCube
    {
        //#region POC
        ////List<BasicStructs.Point> newGhostStruct_POC = GhostStruct.DoPoc_NativeStructToGhostStruct();
        ////List<int> newNativeStruct_POC = NativeStruct.DoPoc_GhostStructToNativeStruct();

        ////List<int> newNativeStruct = NativeStruct.DoGhostStructToNativeStruct(newGhostStruct_POC);
        ////List<BasicStructs.Point> newGhostStruct = GhostStruct.DoNativeStructToGhostStruct(newNativeStruct);


        //////Compara resultados da POC com Função nominal
        ////bool boolNativeStruct = true;
        ////for (int i = 0; i < newNativeStruct_POC.Count(); i++)
        ////{
        ////    if (newNativeStruct_POC[i] != newNativeStruct[i])
        ////    {
        ////        boolNativeStruct = false;
        ////        break;
        ////    }
        ////}
        ////Console.WriteLine(boolNativeStruct);


        //////Compara resultados da POC com Função nominal
        ////bool boolGhostStruct = true;
        ////for (int i = 0; i < newGhostStruct_POC.Count(); i++)
        ////{
        ////    if (
        ////        (newGhostStruct_POC[i].x != newGhostStruct_POC[i].x) &&
        ////            (newGhostStruct_POC[i].y != newGhostStruct_POC[i].y) &&
        ////            (newGhostStruct_POC[i].z != newGhostStruct_POC[i].z)
        ////            )
        ////    {
        ////        boolGhostStruct = false;
        ////        break;
        ////    }
        ////}
        ////Console.WriteLine(boolGhostStruct);
        //#endregion POC
    }
}
